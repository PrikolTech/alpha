components:
  schemas:
    Error:
      type: object
      description: Общая ошибка
      required:
        - code
        - message
      properties:
        code:
          type: integer
        message:
          type: string
